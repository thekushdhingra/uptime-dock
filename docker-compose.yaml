services:
  backend:
    build: ./backend

  frontend:
    build: ./frontend
    ports:
      - "3112:3112"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
